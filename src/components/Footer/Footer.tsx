
import styles from "./footer.module.scss";

const Footer = () => {
	return (
		<footer className={styles.footer}>
			<p className={styles.description}>
				This application is a Progressive Web App (PWA) designed to manage all your web applications in one place with a universal search option.
			</p>
			
		</footer>
	);
};

export default Footer;
